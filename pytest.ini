[pytest]
markers =
    correctness: validates implementation against the spec (spec violations cause failures)
    regression: locks in observed behaviour to catch unintended breakage
    slow: long-running checks (e.g. Monte-Carlo self-consistency); skip with -m "not slow"
